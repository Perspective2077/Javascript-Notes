TODO FOR ADMIN : 
datatypes , aall types are object , refine it ...
object keyss, direct values without keys
Arrow fnc, withoutt brackets using arrow fnc
crypto Api need to add inside math api
function* need to ad
Promises EXPLANATION + How they works
async func always return a promise .. no matter what.. if u used the async keyword fnc going to retrun an promise
Windows.location more features 
nested callback functions
Array destructure example,output should be added
event delegations + event keywords
getBoundingClientRect() dom event
multiple variable deffinging in one line
find method in iterables loop
Map object constructor
single array automatic conversion in string by javascript neeeds to be added 
document.activeElement in event sections
we call object also an object
promise wrapping
 When you import something from a JavaScript (or TypeScript) module, the entire file is executed immediately as it’s imported.
storing new values in object
#Create new folder for adding tips 
attribute wagera wagera
Techincall terms
add computed property names insi

localstrage and other data that stores string as values... if u want to store js code therer . u might want to parse it.. json parsing info need to be added

IntersectionObserver ( need to add in Event section )



add websocket working 

justice for js docs.. don't have time :(
